<?php

class Channel implements Structure {

	/**
	 * Render the frontend channel form
	 *
	 * @param   ChannelEntry            $entry      Current channel entry
	 *                      or blank for new entry
	 * @param	ParsedTemplate|string	$template	The parsed template from
	 * 						the template engine or a string of tagdata.
	 */
	public function renderForm($entry, $template)
	{
		// ...
	}

}